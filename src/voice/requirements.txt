azure-ai-voicelive[aiohttp]==1.1.0
azure-identity==1.19.0
httpx==0.27.2
# Microsoft's official VoiceLive samples use PyAudio (24 kHz PCM16
# mono with callback streaming). On Windows there's a wheel; on Linux
# you need `sudo apt-get install -y portaudio19-dev libasound2-dev`
# before `pip install`. macOS: `brew install portaudio`.
pyaudio>=0.2.14
numpy==2.1.3
