"""NOCLAR agent package — built on the Azure AI Foundry Agent Service.

Submodules:
  * `intake_agent`     — guides the structured NOCLAR intake
  * `grounded_agent`   — RAG-style agent with Foundry IQ + Content Understanding
  * `orchestrator`     — multi-agent + HITL workflow that produces an AssessmentMemo
  * `tools.functions_tools` — function-tool wrappers around the Functions HTTP endpoints
"""
