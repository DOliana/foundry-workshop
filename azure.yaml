# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: foundry-workshop
metadata:
  template: foundry-workshop@1.0.0

infra:
  provider: bicep
  path: infra
  module: main

services:
  functions:
    project: ./src/functions
    language: python
    host: function
